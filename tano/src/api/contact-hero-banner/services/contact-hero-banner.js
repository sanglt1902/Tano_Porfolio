'use strict';

/**
 * contact-hero-banner service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::contact-hero-banner.contact-hero-banner');
