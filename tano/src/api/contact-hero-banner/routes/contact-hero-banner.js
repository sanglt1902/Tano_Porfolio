'use strict';

/**
 * contact-hero-banner router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::contact-hero-banner.contact-hero-banner');
